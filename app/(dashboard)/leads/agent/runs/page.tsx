import type { Metadata } from "next";

export const metadata: Metadata = { title: "Leads Agent — Runs" };

export default function LeadsAgentRunsPage() {
  return (
    <div className="p-8 bg-gray-50 rounded-lg border border-dashed border-gray-300 text-center">
      <p className="text-gray-600">Rungeschiedenis en taakstatus worden in een volgende fase gebouwd.</p>
    </div>
  );
}
